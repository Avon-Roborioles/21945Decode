package org.firstinspires.ftc.teamcode.Utility;

import com.pedropathing.geometry.Pose;

public class PosStorage {
    public static class memory {
        public static Pose lastPose = new Pose(72, 72, Math.toRadians(270));
    }
}
